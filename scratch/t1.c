// april 12, 2026
#include <stdio.h> //including information about a library (standard input/output in this case)
int main() //defining a function called main that receives no argument values, the statements of main are enclosed in braces ({/})
{
    printf("hello, world\n"); //main() calls library function printf to print a sequence of characters
}
